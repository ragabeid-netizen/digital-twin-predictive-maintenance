# Python 3.11 — exact versions used to produce the reported results.
# (Loosen the pins to >= if you only need a compatible environment.)
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
xgboost==3.2.0
statsmodels==0.14.6
prophet==1.3.0
tensorflow==2.21.0
torch==2.12.1
shap==0.51.0
matplotlib==3.11.0
openpyxl==3.1.5
